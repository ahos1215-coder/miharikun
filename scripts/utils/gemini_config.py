"""
Gemini API 共通設定 — Single Source of Truth
============================================
全モジュールはここから設定を import すること。
モジュールごとに定数を重複定義せず、このファイルを唯一の定義元とする。
"""

import os

# ---------------------------------------------------------------------------
# モデル設定
# ---------------------------------------------------------------------------

DEFAULT_PRIMARY_MODEL: str = os.environ.get("GEMINI_MODEL", "gemini-2.5-flash")
DEFAULT_FALLBACK_MODEL: str = os.environ.get("GEMINI_FALLBACK_MODEL", "gemini-2.0-flash")

# ---------------------------------------------------------------------------
# API 設定
# ---------------------------------------------------------------------------

GEMINI_API_BASE: str = "https://generativelanguage.googleapis.com/v1beta/models"
GEMINI_API_KEY: str = os.environ.get("GEMINI_API_KEY", "")

# ---------------------------------------------------------------------------
# リトライ設定
# ---------------------------------------------------------------------------

MAX_RETRIES: int = 6
BASE_WAIT: float = 1.0
MAX_WAIT: float = 32.0

# ---------------------------------------------------------------------------
# 生成設定
# ---------------------------------------------------------------------------

DEFAULT_TEMPERATURE: float = 0.1

# マッチング用（低い温度で決定的な判断）
MATCHING_TEMPERATURE: float = 0.1

# 分類用（少し高い温度で多様な出力）
CLASSIFICATION_TEMPERATURE: float = 0.2

# ---------------------------------------------------------------------------
# レートリミッター設定
# ---------------------------------------------------------------------------

# Tier 1: 15 RPM / 1500 RPD
MIN_REQUEST_INTERVAL: float = float(os.environ.get("GEMINI_MIN_INTERVAL", "4.0"))
